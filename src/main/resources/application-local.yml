spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: '!present2@'
    hikari:
      schema: schema

const:
  base-dir: 'C:/Users/taehoon.kim/dev/uploadFiles'

server:
  port: 58080
