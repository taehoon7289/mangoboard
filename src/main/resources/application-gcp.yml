spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://mysql8.0:3306?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: '!present2@'
    hikari:
      schema: schema

const:
  base-dir: '/app/uploadFiles'

server:
  port: 58080
